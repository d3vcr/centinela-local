# Referencia visual — fuente pendiente

Estado: `FALTANTE`.

Se observaron capturas de prueba `tests/visual/cockpit-*.png` y wireframes
`docs/ux/wireframes/*.svg`, pero ninguna fuente las identifica como la imagen
original de referencia solicitada.

No se creó `07_Interfaces/REFERENCIA-DASHBOARD.png` para evitar una sustitución
por similitud. El archivo original debe incorporarse con su nombre, origen,
fecha y hash.
